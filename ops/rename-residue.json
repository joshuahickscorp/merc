{
  "schema_version": 1,
  "kind": "rename_residue_audit",
  "counts": {
    "FROZEN": 188,
    "BLOCKED": 391,
    "RESIDUE": 6
  },
  "blocked_by_prerequisite": {
    "metric name in the alert label set; must land with the receiver update": 96,
    "binary name in Dockerfile.control, CI SHA256SUMS, .gitignore, .dockerignore and the rollback image entrypoint": 66,
    "object storage bucket name in a running deployment": 24,
    "model alias inside the sha256'd vLLM runtime profile; workers must re-attest in the same cutover": 21,
    "systemd unit installed under this name; disable the old timer first": 18,
    "published image tag": 15,
    "real directory on supplier machines": 15,
    "droplet .env, GitHub Actions secrets and systemd units supply these; CX_TOKEN_KEY must be copied byte-identically or every sealed secret in Postgres becomes undecryptable": 15,
    "rename the GitHub repository first": 12,
    "real directory on the droplet": 11,
    "alert names ship in the label set Alertmanager fingerprints": 11,
    "static binary baked into the published image and named in the compose healthcheck; must move with the image": 10,
    "live production domain": 7,
    "Prometheus job label fingerprinted by Alertmanager": 7,
    "bind-mount path in a running deployment": 6,
    "rename/republish the registry package first": 6,
    "local rehearsal container name": 5,
    "the cx CLI binary name": 4,
    "external asset pack name; the comment must keep matching it": 4,
    "real directory on the staging server": 4,
    "launchd label on already-installed agents": 4,
    "generated asset filename": 4,
    "generated asset filename; the ignore pattern must keep matching what the builders emit": 3,
    "real directory on supplier machines (two-e spelling)": 2,
    "version field inside the sha256'd vLLM runtime profile": 2,
    "Postgres role in a running deployment": 2,
    "Prometheus external_labels.service; Alertmanager fingerprints the label set and the receiver filters on it": 2,
    "real S3 prefix holding staging backups; renaming orphans them": 2,
    "prove-local artifact namespace recorded in evidence": 2,
    "logo file pinned BY PATH AND SHA256 in ops/asset-provenance.json; moving it orphans the receipt": 2,
    "label on Stripe webhook endpoints that already exist; part of the Stripe-label cutover": 2,
    "soak run directory name; an in-flight 24h soak writes there": 2,
    "restore-drill container name": 2,
    "former Go module path in recorded output": 1,
    "Prometheus job label in the fingerprinted set": 1,
    "Grafana datasource uid referenced by every dashboard panel": 1
  },
  "residue": [
    {
      "file": "scripts/rename-residue-audit.py",
      "line": 2,
      "token": "ComputeXchange",
      "context": "\"\"\"Zero-residue audit for the ComputeXchange -> merc rename."
    },
    {
      "file": "scripts/rename-residue-audit.py",
      "line": 39,
      "token": "ComputeXchange",
      "context": "r\"ComputeXchange|Computeexchange|computexchange|computeexchange|\""
    },
    {
      "file": "scripts/rename-residue-audit.py",
      "line": 39,
      "token": "Computeexchange",
      "context": "r\"ComputeXchange|Computeexchange|computexchange|computeexchange|\""
    },
    {
      "file": "scripts/rename-residue-audit.py",
      "line": 39,
      "token": "computexchange",
      "context": "r\"ComputeXchange|Computeexchange|computexchange|computeexchange|\""
    },
    {
      "file": "scripts/rename-residue-audit.py",
      "line": 39,
      "token": "computeexchange",
      "context": "r\"ComputeXchange|Computeexchange|computexchange|computeexchange|\""
    },
    {
      "file": "scripts/rename-residue-audit.py",
      "line": 179,
      "token": "computexchange",
      "context": "# matched by the bare `computexchange` alternative first)."
    }
  ],
  "residue_total": 6,
  "zero_residue": false,
  "note": "FROZEN and BLOCKED are deliberate. FROZEN must never be renamed; BLOCKED needs an external change first and renaming in-repo before it produces a dangling reference that still looks plausible. Only RESIDUE is a defect."
}
