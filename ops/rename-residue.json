{
  "schema_version": 1,
  "kind": "rename_residue_audit",
  "counts": {
    "FROZEN": 333,
    "BLOCKED": 480,
    "RESIDUE": 0
  },
  "blocked_by_prerequisite": {
    "metric name in the alert label set; must land with the receiver update": 256,
    "model alias inside the sha256'd vLLM runtime profile; workers must re-attest in the same cutover": 54,
    "object storage bucket name in a running deployment": 44,
    "droplet .env, GitHub Actions secrets and systemd units supply these; CX_TOKEN_KEY must be copied byte-identically or every sealed secret in Postgres becomes undecryptable": 30,
    "published image tag": 21,
    "alert names ship in the label set Alertmanager fingerprints": 19,
    "real directory on the droplet": 6,
    "bind-mount path in a running deployment": 6,
    "Prometheus job label fingerprinted by Alertmanager": 6,
    "rename/republish the registry package first": 5,
    "the cx CLI binary name": 4,
    "live production domain": 4,
    "Postgres role in a running deployment": 4,
    "local rehearsal container name": 4,
    "real directory on the staging server": 3,
    "generated asset filename; the ignore pattern must keep matching what the builders emit": 2,
    "external asset pack name; the comment must keep matching it": 2,
    "generated asset filename": 2,
    "Prometheus external_labels.service; Alertmanager fingerprints the label set and the receiver filters on it": 1,
    "prove-local artifact namespace recorded in evidence": 1,
    "restore-drill container name": 1,
    "soak run directory name; an in-flight 24h soak writes there": 1,
    "label on Stripe webhook endpoints that already exist; part of the Stripe-label cutover": 1,
    "real S3 prefix holding staging backups; renaming orphans them": 1,
    "real directory on supplier machines (two-e spelling)": 1,
    "version field inside the sha256'd vLLM runtime profile": 1
  },
  "residue": [],
  "residue_total": 0,
  "zero_residue": true,
  "note": "FROZEN and BLOCKED are deliberate. FROZEN must never be renamed; BLOCKED needs an external change first and renaming in-repo before it produces a dangling reference that still looks plausible. Only RESIDUE is a defect."
}
