# Large performance receipts stay in Git LFS so the source tree remains usable
# without downloading every measurement payload.
evidence/perf/** filter=lfs diff=lfs merge=lfs -text
ops/tooling/runpodctl filter=lfs diff=lfs merge=lfs -text
