# Ocular browser benchmark (Phase N)

Contradiction detectors are demonstrated on a synthetic multi-surface snapshot.
Physical capture is optional and must use `scripts/with-one-browser.sh`.

```bash
.venv/bin/python scripts/run-ocular-browser.py --output artifacts/ocular/browser
```
