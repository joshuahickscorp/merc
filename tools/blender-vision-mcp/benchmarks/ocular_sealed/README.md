# Ocular sealed benchmarks (Phase S)

Eight sealed targets materialised under the runner output
(`artifacts/ocular/sealed/benchmarks/`). Single split authority; leakage canaries.

```bash
.venv/bin/python scripts/run-ocular-sealed.py --output artifacts/ocular/sealed
```
