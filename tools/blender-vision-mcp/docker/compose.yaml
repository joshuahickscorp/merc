services:
  vision:
    build:
      context: ..
      dockerfile: docker/vision.Dockerfile
    read_only: true
    network_mode: none
    stdin_open: true
    volumes:
      - projects:/projects
      - /tmp
    environment:
      BVMCP_PROJECTS_ROOT: /projects

volumes:
  projects:

