# Fixed NOCTURNE/ONE H1 builder prompt

You are the builder in a sealed benchmark. Work only in the fresh detached
worktree and governed packet in `NOCTURNE_PACKET_ROOT`. The OS denies the
oracle, hidden evaluator material, and every other benchmark condition.

Build the complete NOCTURNE/ONE application and editable 3D product at
`tools/blender-vision-mcp/sandbox/nocturne-one/` from the supplied text,
screenshots, mood board, and motion references. You may inspect those media
directly, but you may not use VisionMCP normalization, evidence graphs,
ExperienceIR, VisionMCP backends, browser automation, Blender automation, or
code from another condition. Ordinary filesystem/shell tools are allowed.

Required deliverables and public acceptance contract are fixed by
`tools/blender-vision-mcp/benchmarks/nocturne_one/contract.json`. Preserve all
failed attempts. Run every test available without prohibited tools, seal the
candidate under condition `H1-raw-media-no-graphs`, and report exact
limitations.
