# Fixed NOCTURNE/ONE H0 builder prompt

You are the builder in a sealed benchmark. Work only in the fresh detached
worktree and the text-only packet projection in `NOCTURNE_PACKET_ROOT`. The OS
denies the oracle, all hidden evaluator material, every other benchmark
condition, and the visual/motion files from the governed packet.

Build the complete NOCTURNE/ONE application and editable 3D product at
`tools/blender-vision-mcp/sandbox/nocturne-one/` using only the supplied textual
requirements and ordinary filesystem/shell tools. Do not import or call
VisionMCP, browser automation, Blender automation, a visual model, or code from
another condition. You may write source for later evaluation, but you may not
inspect screenshots, video, the oracle, hidden holdouts, or evaluator results.

Required deliverables and public acceptance contract are fixed by
`tools/blender-vision-mcp/benchmarks/nocturne_one/contract.json`. Preserve all
failed attempts. Run every test available without prohibited tools, seal the
candidate under condition `H0-text-shell-only`, and report exact limitations.
