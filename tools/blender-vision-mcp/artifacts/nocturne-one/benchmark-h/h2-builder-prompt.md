# Fixed NOCTURNE/ONE H2 builder prompt

You are the builder in a sealed benchmark. Work only in the fresh detached
worktree and governed packet in `NOCTURNE_PACKET_ROOT`. The OS denies the
oracle, hidden evaluator material, and every other benchmark condition.

Build the complete NOCTURNE/ONE application and editable 3D product at
`tools/blender-vision-mcp/sandbox/nocturne-one/` from all governed text and
visual references. Raw browser and Blender automation are allowed. VisionMCP
normalization, evidence graphs, ExperienceIR, candidate authorities, bounded
repair authorities, and code from another condition are prohibited.

Required deliverables and public acceptance contract are fixed by
`tools/blender-vision-mcp/benchmarks/nocturne_one/contract.json`. Preserve all
failed attempts, run real browser/Blender/application tests, seal the candidate
under condition `H2-raw-browser-blender`, and report exact limitations.
