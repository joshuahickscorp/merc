# Audit an existing model

Open the project, inspect its evidence and scene inventory, then run
`workflow.audit_reference_fidelity`. Poll the returned job to a terminal state. Report camera
authority, residual metrics, coverage, uncertainty, and every acceptance blocker. Never present an
approximate registration as a metric camera solution.
