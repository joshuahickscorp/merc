# Worker operations

Authoritative worker operations are implemented in the packaged, self-contained
`blender_vision.blender.worker_entry` module. This directory is reserved for operations that
grow large enough to vendor into Blender job bundles.

